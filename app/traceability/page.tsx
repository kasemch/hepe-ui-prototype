import { loadPilotReadModel } from '../../lib/hepe/pilot-read';
export default async function TracePage({searchParams}:{searchParams:Promise<{plo?:string;irm?:string;q?:string;mapped?:string}>}){const m=await loadPilotReadModel();const sp=await searchParams;if(m.state!=='READY')return <main className="workspace"><div className="module-wrap"><a className="breadcrumb" href="/">← Academic Command Center</a><section className="hero"><div className="brand-kicker">HEPE · NON-PRODUCTION</div><h1>PLO / Course Traceability</h1><p>Authority-aware traceability surface.</p></section><div className="pill">{m.state}</div></div></main>;const q=(sp.q??'').toLowerCase();const mappedSet=new Set(m.mappings.map((x:any)=>x.course_code));const courseRows=m.courses.filter((c:any)=>!q||String(c.course_code).toLowerCase().includes(q)||String(c.title_th??'').toLowerCase().includes(q));const links=m.mappings.filter((x:any)=>(!sp.plo||x.plo_code===sp.plo)&&(!sp.irm||x.irm_level===sp.irm)&&(!q||String(x.course_code).toLowerCase().includes(q)||String(x.title_th??'').toLowerCase().includes(q)));const filteredCourses=courseRows.filter((c:any)=>sp.mapped==='yes'?mappedSet.has(c.course_code):sp.mapped==='no'?!mappedSet.has(c.course_code):true);return <main className="workspace"><div className="module-wrap"><a className="breadcrumb" href="/">← Academic Command Center</a><section className="hero"><div className="brand-kicker">HEPE · CONTROLLED PILOT · NON-PRODUCTION</div><h1>PLO / Course Traceability</h1><p>Approved Course→PLO I-R-M relationships only. No inferred relationship is added.</p></section><div className="pill">7 PLO · 99 PAIRS · 78 MAPPED · 14 UNMAPPED · I9 / R72 / M18</div><form className="filterbar" method="get"><label>PLO<select name="plo" defaultValue={sp.plo??''}><option value="">All</option>{['PLO1','PLO2','PLO3','PLO4','PLO5','PLO6','PLO7'].map(x=><option key={x}>{x}</option>)}</select></label><label>I-R-M<select name="irm" defaultValue={sp.irm??''}><option value="">All</option>{['I','R','M'].map(x=><option key={x}>{x}</option>)}</select></label><label>Mapped<select name="mapped" defaultValue={sp.mapped??''}><option value="">All</option><option value="yes">Mapped</option><option value="no">Unmapped</option></select></label><label>Search<input name="q" defaultValue={sp.q??''}/></label><button type="submit">Apply</button></form><section className="card"><h2 className="section-title">Course→PLO relationships</h2><div className="table-wrap"><table><thead><tr><th>PLO</th><th>Course</th><th>Thai title</th><th>Level</th><th>Source</th></tr></thead><tbody>{links.map((x:any)=><tr key={x.course_plo_mapping_id}><td>{x.plo_code||'NOT PROVIDED'}</td><td><strong>{x.course_code}</strong></td><td>{x.title_th||'NOT PROVIDED'}</td><td>{x.irm_level}</td><td>{x.source_page?`p.${x.source_page}`:'PROVENANCE NOT AVAILABLE'}</td></tr>)}</tbody></table></div></section><section className="card" style={{marginTop:14}}><h2 className="section-title">Mapped / Unmapped Course Summary</h2><div className="table-wrap"><table><thead><tr><th>Course</th><th>Thai title</th><th>Status</th></tr></thead><tbody>{filteredCourses.map((c:any)=><tr key={c.curriculum_course_id}><td>{c.course_code}</td><td>{c.title_th||'NOT PROVIDED'}</td><td>{mappedSet.has(c.course_code)?'MAPPED':'UNMAPPED — NO RELATIONSHIP INFERRED'}</td></tr>)}</tbody></table></div></section><div className="firewall">Visible relationships: {links.length} · Controlled total: 99 · Backend/RLS remains authoritative.</div></div></main>}
