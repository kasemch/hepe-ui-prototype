name: HEPE REL-03D.3G.2 SSR Cookie Diagnostic v2

on:
  push:
    branches: [feat/hepe-rel-03-runtime-binding]
    paths: ['.github/workflows/hepe-rel-03d3g2-cookie-diagnostic-v2.yml']

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      SUPABASE_URL: https://lztxpjsuzqvtgyasfnyj.supabase.co
      SUPABASE_PUBLISHABLE_KEY: sb_publishable_7bv5GR0-ksXJn91sRHV0Mg_k4nblIGI
      SUPABASE_ADMIN: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      VERCEL_BYPASS: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
      PREVIEW_URL: https://hepe-ui-prototype-q8wncez6v-kasemch-3467s-projects.vercel.app
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - run: |
          set -euo pipefail
          test -n "${SUPABASE_ADMIN:-}" || exit 78
          test -n "${VERCEL_BYPASS:-}" || exit 78
          npm install --ignore-scripts --no-audit --no-fund
          node scripts/hepe-rel03-cookie-diagnostic.mjs
      - if: always()
        run: echo 'NON-PRODUCTION synthetic diagnostic only; cleanup is mandatory.'
